<UserControl x:Class="code_in_test.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible" x:Name="scroll">

    <Grid x:Name="grid_win" Background="Black" Width="1000" Height="1000" MouseMove="UserControl1_MouseMove" KeyDown="grid_win_KeyDown" Focusable="True">
            <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Create node" Click="onClickCreate" x:Name="MenuItemNode">
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
    </ScrollViewer>
</UserControl>
