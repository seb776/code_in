<UserControl x:Class="code_in.Views.ConfigView.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <!-- Code un peu plus propre, je n'arrive juste pas encore a utiliser le treeview comme je veux :/ -->
    <!-- Treeview du menu = Sommaire ^^ #first try tree -->
        <TreeView  Margin="0,50,0,0" Width="200" VerticalAlignment="Stretch" HorizontalAlignment="Left" Background="#ebebeb" FontSize="18">
            <TreeViewItem Header="Général" Foreground="#424242" />
            <TreeViewItem Header="Thèmes" IsExpanded="True" Foreground="#424242" >
                <TreeViewItem Header="Changer de thème" Foreground="#424242"/>
                <TreeViewItem Header="Créer un thème" Foreground="#424242"/>
            </TreeViewItem>
            <TreeViewItem Header="Erreurs" Foreground="#424242" />
            <TreeViewItem Header="Raccourcis" Foreground="#424242"/>
            <TreeViewItem Header="Performances" Foreground="#424242"/>
        </TreeView>
       <!-- Panneau de droite = contenu des menus -->
        <StackPanel Margin="200, 50, 0, 0" Background="#424242" VerticalAlignment="Stretch">
            
        </StackPanel>
        
        <!-- Bannière de haut de page = information sur la catégorie séléctionnée, faite en 2 textblock pour pouvoir changer dynamiquement le nom de la catégorie séléctionnée -->
        <StackPanel Margin="0, 0, 0, 0" Height="50" HorizontalAlignment="Stretch" VerticalAlignment="Top" Background="#424242">
            <TextBlock Text="Configuration :" FontSize="20" Foreground="#ebebeb" Width="200" HorizontalAlignment="Left"/>
            <TextBlock Text="DYNAMIC !" Margin="200,-25,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Height="50" FontSize="20" Foreground="#ebebeb"/>
        </StackPanel>
    </Grid>
</UserControl>