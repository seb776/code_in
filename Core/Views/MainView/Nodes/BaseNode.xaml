<UserControl x:Class="code_in.Views.MainView.Nodes.BaseNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="250"
             MinWidth="250" MinHeight="150"
             xmlns:my="clr-namespace:code_in.Views.MainView.Nodes">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical">
            <Label Foreground="GreenYellow">Modifier1</Label>
            <Label Foreground="GreenYellow">Modifier2</Label>
        </StackPanel>
        <Border Grid.Row="1" BorderThickness="2" BorderBrush="GreenYellow">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25"/>
            </Border.Effect>
            <Grid Background="#424242">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Rectangle Grid.Row="0" Fill="GreenYellow"/>
                <Label Grid.Row="0" FontWeight="Bold">NodeName</Label>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <my:IOItem x:Name="InFlow"></my:IOItem>
                        <my:IOItem></my:IOItem>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <my:IOItem x:Name="OutFlow" Orientation="1"></my:IOItem>
                        <my:IOItem Orientation="1"></my:IOItem>
                    </StackPanel>
                    <Polygon Grid.Column="1" Fill="GreenYellow" Points="15,15 0,15 15,0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Cursor="SizeNWSE" PreviewMouseLeftButtonDown="Polygon_PreviewMouseLeftButtonDown"/>
                    <!--<Rectangle Fill="GreenYellow" Grid.Column="1"/>-->
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
